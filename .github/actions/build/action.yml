name: 'Build Application/Service'
description: 'Build Sample Book App service'
runs:
  using: composite
  steps:
    - run: echo "Installing all necessary node dependencies.."
      shell: bash
    - name: Install dependecies
      run: npm install
      shell: bash
    - run: echo "Dependecies successfully installed.."
      shell: bash